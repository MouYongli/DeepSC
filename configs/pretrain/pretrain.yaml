defaults:
  - _self_
  - model: scbert
  - dataset: tripleca

wandb_project: "DeepSC"

data_path: "/home/angli/baseline/DeepSC/data/cellxgene/test_normalized"
seed: 42
ckpt_dir: "/home/angli/baseline/DeepSC/ckpts/"
num_device: 4
valid_every: 1
model_name: "scbert"
grad_acc: 80
learning_rate: 1e-4
epoch: 6
batch_size: 7
num_bin: 5
save_ckpt_every: 100

hydra:
  # Logging configuration: https://hydra.cc/docs/tutorials/basic/running_your_app/logging/
  verbose: False
  # Logs to stdout and to a file.
  job_logging:
    handlers:
      console:
        class: logging.StreamHandler
        stream: ext://sys.stdout
      file:
        filename:
          ${hydra.runtime.output_dir}/${hydra.job.name}_${oc.select:hydra.job.num, 0}.log

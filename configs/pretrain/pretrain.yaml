defaults:
  - _self_
  - model: deepsc
  - dataset: tripleca

wandb_project: "DeepSC"

data_path: "/home/angli/baseline/DeepSC/data/cellxgene/intestine_partition_0_sparse.pth"
seed: 42
ckpt_dir: "/home/angli/baseline/DeepSC/ckpts/"
num_device: 2
valid_every: 1
model_name: "scbert"
grad_acc: 20
learning_rate: 5e-4
epoch: 20
batch_size: 10
num_bin: 8
save_ckpt_every: 1000

hydra:
  # Logging configuration: https://hydra.cc/docs/tutorials/basic/running_your_app/logging/
  verbose: False
  # Logs to stdout and to a file.
  job_logging:
    handlers:
      console:
        class: logging.StreamHandler
        stream: ext://sys.stdout
      file:
        filename:
          ${hydra.runtime.output_dir}/${hydra.job.name}_${oc.select:hydra.job.num, 0}.log

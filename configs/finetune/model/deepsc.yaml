_target_: src.deepsc.models.deepsc_new.model.DeepSC
embedding_dim: 256
num_genes: 34683
num_layers: 10
mask_layer_start: 100
num_heads: 8
use_new_ffn: False
ffn_dim: 1024
attn_dropout: 0.1
ffn_dropout: 0.1
fused: False
ffn_layers: 2
calculate_M: False
num_bins: ${num_bin}
enable_l0: ${enable_l0}
enable_mse: ${enable_mse}
enable_ce: ${enable_ce}
use_gene_emb: True
use_expr_emb: True
